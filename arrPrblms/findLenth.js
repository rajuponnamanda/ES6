// Find length of the arrar without using length //

var findLingth = [12, 123, 14, 15, 16];
console.log(findLingth.length);

// Now trying find length of the array with out using length //

let finalLen = [12, 13, 14, 15, 16, 17];

console.log(arrayLength(finalLen));
function arrayLength(s) {
  let length = 0;
  while (finalLen[length] !== undefined) {
    return length++;
  }
}
